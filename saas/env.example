# SaaS Project Environment Variables Template
# Copy this file to .env-dev, .env-tst, or .env-pro and modify values as needed

# PostgreSQL Configuration
POSTGRES_DB=saas_db
POSTGRES_USER=rest_user
POSTGRES_PASSWORD=rest_pass

# PostgREST Configuration
PGRST_DB_URI=postgres://rest_user:rest_pass@localhost:5432/saas_db
PGRST_DB_SCHEMA=public
PGRST_DB_ANON_ROLE=anon
PGRST_JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long
PGRST_JWT_AUD=saas
PGRST_JWT_EXP=1h

# Service Ports
POSTGRES_PORT=5432
POSTGREST_PORT=3000

# Development Settings
NODE_ENV=development
LOG_LEVEL=info

# Security Settings (Production Only)
# SSL_MODE=require
# MAX_CONNECTIONS=100
# CONNECTION_TIMEOUT=30
